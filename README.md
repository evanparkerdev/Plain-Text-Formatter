# Text Cleaner

Small static web app that cleans up pasted text:

- Trims line edges
- Collapses repeated spaces/tabs
- Collapses repeated blank lines
- Shows cleaned output and includes a copy button

## Run locally

From this folder:

```bash
node server.js
```

Then open:

- `http://localhost:5173/`
